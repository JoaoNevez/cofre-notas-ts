# Cofre de Notas Criptografadas (TypeScript)

Aplicação simples para guardar notas criptografadas no navegador usando AES-GCM e PBKDF2.

## Como rodar

```bash
npm install
npm run build
npm run dev
```

Depois abra [http://localhost:3000](http://localhost:3000).
